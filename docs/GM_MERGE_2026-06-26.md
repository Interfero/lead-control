# GM merge report - 2026-06-26

## Source and target

- Original LC base: `C:\Users\666\Desktop\lead-control.space.app`
- Prepared release copy: `C:\CRM_MAIN\LC_GM_RELEASE_20260626\lead-control.space.app`
- GM reference source: `C:\CRM_MAIN\LC_ 19.06_download\lead-control.space.app`

The original Desktop folder was not edited. The prepared release copy was created separately.

## What was preserved

- Existing LC code from the Desktop version was kept as the base.
- Existing Mango webhook API routes were preserved.
- Existing SuperPart API routes and config were preserved.
- Existing CFM/document/review/order logic was not replaced wholesale.
- `.env`, `.git`, logs, sessions and runtime cache were not copied into the release folder.
- Database, migrations and storage data were not touched.

## What was restored for GM

- `routes/api.php`
  - Added `GET /api/v1/health`.
  - Added protected `/api/v1/gm/*` routes.
  - Kept existing Mango and SuperPart routes.

- `config/services.php`
  - Added `services.gm_api` config block with env names only.
  - No real tokens, passwords or secrets were written.

- `app/Http/Controllers/HrController.php`
  - Added `GmUserSyncService`.
  - Added GM credentials generation for HR edit page.
  - Added LC -> GM employee sync on create, update, fire and restore.

- `resources/views/hr/create.blade.php`
  - Added GM login/password display after creating an employee.
  - Added copy buttons for GM login/password.

- `resources/views/hr/edit.blade.php`
  - Added GM login/password fields in employee card.
  - Added copy buttons for GM login/password.

- `app/Http/Controllers/OrderController.php`
  - Added `GmOrderSyncService`.
  - Added LC -> GM order sync after important order field updates.
  - Added LC -> GM order sync after order complete and reopen.

- `app/Http/Controllers/DocumentController.php`
  - Added GM document delete sync when an order document is deleted in LC.
  - Added follow-up order sync after document deletion.

- `app/Models/Order.php`
  - Added status `review`.
  - Label: `Проверка`.
  - No migration was added because `order_status` is already a string field.

- `app/Services/OrderVisibilityService.php`
  - Added `review` to visible/selectable status lists.

## GM classes/docs

These GM files were already present in the Desktop LC base and were left in place:

- `app/Http/Controllers/Api/GmController.php`
- `app/Http/Middleware/VerifyGmApiBearer.php`
- `app/Services/GmApiService.php`
- `app/Services/GmOrderSyncService.php`
- `app/Services/GmUserSyncService.php`
- `docs/gm-api-mapping.md`
- `docs/gm-api-openapi.yaml`

## Not transferred / not changed

- No DB schema changes.
- No migrations.
- No `.env` values.
- No production secrets.
- No composer/npm dependency installation.
- No server cache clear was executed locally.

## Local verification

- `php -l` passed for changed PHP files:
  - `routes/api.php`
  - `config/services.php`
  - `app/Http/Controllers/HrController.php`
  - `app/Http/Controllers/OrderController.php`
  - `app/Http/Controllers/DocumentController.php`
  - `app/Models/Order.php`
  - `app/Services/OrderVisibilityService.php`

## After upload

On the server, after replacing files, clear Laravel cache from the LC project root:

```bash
php artisan optimize:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
```

If hosting uses a versioned binary, use the server's PHP command, for example `php83 artisan ...`.
