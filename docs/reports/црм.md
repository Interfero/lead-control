# Lead Control — отчёт о работах

**Период:** 24–30 августа 2026  
**Prod:** https://lead-control.space · VPS `reg-vps` (`194.67.92.69`)

---

## Продукт и prod

### Уведомления о новых заявках (CRM + Desk)

- Доработан звук: `public/js/lc-notify-sound.js` — двухтональный chime ~1.25 с вместо обрывающегося beep (~0.45 с).
- Убран весь тестовый UI (жёлтая полоска, FAB, кнопки «Тест звука», служебные meta/route).
- Звук работает в фоне через `city-order-notify.js` (CRM) и `desk-order-notify.js` (Desk).
- Деплой на `/var/www/lead-control` и `/var/www/lead-desk`, перезагрузка `php8.2-fpm`.
- Тестовые заявки на Рязань (№450–458): создание → проверка звука → удаление.

### Единое окно (Desk)

- Исправлен **HTTP 500** после снятия тест-кнопок: незакрытый `@if` в `lead-desk/resources/views/layouts/app.blade.php`.

### Guild Master (GM)

**27.08** — verify-password:

- Подтверждён prod smoke (`userId=95` → 200, неверный пароль → 401).
- Документация: `docs/GM-VERIFY-PASSWORD.md`.

**28.08** — change-password (ТЗ команды GM):

- `POST /api/v1/gm/users/change-password` — смена пароля мастера (server-to-server).
- `GET /api/v1/gm/users/{userId}/password-status` — баннер принудительной смены.
- Rate limit: 5 попыток / 15 мин на userId+IP.
- Политика пароля как в CRM (min 8, новый ≠ текущий).
- Деплой на prod, документация: `docs/GM-CHANGE-PASSWORD.md`, обновлён `docs/API.md`.
- **Open:** полный E2E на user 95 — временный пароль в личку; прокси на стороне GM.

### Инциденты и поддержка

| Дата | Обращение | Результат |
|------|-----------|-----------|
| 28.08 | «База не работает» | VPS `194.67.92.69` недоступен (ping/SSH/HTTPS timeout). MySQL на `reg-crm` жива. Не баг конфига LC. |
| 29.08 | CRM не грузит через VPN (Happ, Хельсинки) | Сервер OK. Обход: whitelist `lead-control.space` + `194.67.92.69`, скрипты `deploy/fix-lead-control-vpn-lag.ps1` / `.sh`. |
| 30.08 | НН: «база не грузится» (VPN и без) | Prod OK: city_id=50, 289 заказов, страница заказов ~0.11 с, 500 нет. Активность из региона сегодня есть. Мастерам CRM `/orders` → 403 (нужен GM). Ждём login/URL/скрин. |

---

## Документация и инфраструктура

### Obsidian (`S:/Obsidian/Проекты`)

- **25.08:** ключевые заметки Lead Control, карта экосистемы (CRM, Desk, SuperPart, Mango, GM, promo).
- **30.08:** зеркало «командной памяти» в `Lead Control/Командная память/`.

### Командная память (AI-Memory)

- **29.08:** onboarding проекта `lead-control` (GitHub `bannyxax`, vault, MCP, identity).
- Глубокий разбор архитектуры: 7 модулей, 5 интеграций, решения — записано в общую память команды.

### Ops-скрипты (`deploy/vps/`)

- Тесты звука Рязань: `recreate-ryazan-sound-test.php`, `ryazan-sound-final-test.php`, cleanup desk cache.
- GM: `smoke-gm-change-password.sh`.
- Диагностика НН: `diag-nn-branch.php`, `diag-nn-orders.php`, `diag-nn-staff.php`.

---

## Не закрыто / вне скоупа LC

| Задача | Статус |
|--------|--------|
| E2E change-password на prod (user 95) | ждёт пароль в личку |
| GM: UI «Сменить пароль» | команда repair-guild.ru |
| Read-only MySQL для AI (CRM / SuperPart / Virtus) | нужна панель Reg.ru (нет CREATE USER у app-аккаунта) |
| Жалоба НН «не грузит» | ждём детали от филиала |

---

## Итого

**Сделано:** звук уведомлений + fix Desk 500; GM API change-password на prod; документация GM; Obsidian + командная память; диагностика VPS / VPN / НН.

**Prod:** CRM и GM API отвечают; критичных 500 по CRM за 30.08 не зафиксировано.
